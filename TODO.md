# TODO

更新 2026-08-13

**このファイルは「次に何をやるか」だけを書きます。** 完了したものは消し、判断の背景は `docs/` 側に残してください。

---

## 最優先：店舗特定率を実測する

**この数字がサービスの成立可否を決めます。** 事業計画の数値はすべてこれが出るまで暫定値です。

- [ ] **Gemini APIキーを取得する** — [aistudio.google.com/apikey](https://aistudio.google.com/apikey) で即日発行。`scripts/.env` に記入
- [ ] **手元のサンプルで解析を回す** — X APIの承認を待たずに、精度の当たりは付けられる
- [ ] X API 開発者アカウントの承認を待つ／催促する
- [ ] Instagram の App Review（`Instagram Public Content Access`）を申請する
- [ ] **無作為サンプルで店舗特定率を測る**（手順は [docs/5](docs/5_APIキー取得手順と実測手順.md)）
- [ ] 実測値で [docs/4 §0-2](docs/4_一番くじ在庫マップ_データ収集実現可能性調査.md) の予備調査を差し替える
- [ ] 実測値で [docs/6 収益計画書](docs/6_一番くじ在庫マップ_収益計画書.md) を再計算する

> ⚠️ **予備調査（n=24）は仮説であって確定値ではありません。** 「店舗アカウント100% / 一般ユーザー0%」を前提に設計を進める前に、必ず実測してください。

---

## 予備調査を受けた設計変更

[docs/4 §0-2](docs/4_一番くじ在庫マップ_データ収集実現可能性調査.md) で判明した4点の反映。**実測の結果しだいで不要になる可能性があるため、実測後に着手するのが安全です。**

- [ ] **収集クエリを「店舗アカウント中心」に変更する** — 読む投稿数が減るぶん X API のコストも下がる
- [ ] **画像解析（`--vision`）を既定ルートにする** — 店舗アカウントは残数を画像で示すことが多い
- [ ] **Gemini のコストを再試算する** — マルチモーダルはテキストのみより高い。[docs/4 §2-1](docs/4_一番くじ在庫マップ_データ収集実現可能性調査.md) が上振れする
- [x] キーキャップ絵文字（`1️⃣7️⃣`）の正規化 — `pipeline.py` の `strip_keycap()` で対応済み
- [x] ハッシュタグからの店舗・地名抽出 — `mock_analyze()` で対応済み。**Gemini のプロンプト側にも反映が必要**

---

## 要判断：DB は Neon か Supabase か

**ここが決まらないと実装に入れません。** 現状、[デプロイ手順書](docs/9_デプロイ手順書.md)は Neon 前提、コードとその他の文書は Supabase 前提で食い違っています。

- [ ] **どちらを使うか決める**
- [ ] 決まったら、以下をまとめて揃える
  - [ ] `web/src/lib/data/supabase.ts` のファイル名と実装
  - [ ] `data/index.ts` の判定キー（`NEXT_PUBLIC_SUPABASE_URL` か `DATABASE_URL` か）
  - [ ] [技術仕様書](docs/2_一番くじ在庫マップ_技術仕様書.md)・[CONTRIBUTING](CONTRIBUTING.md)・アーキテクチャ図の表記
  - [ ] `scripts/make_diagram.py` の DB ノード名

> Neon を推す理由は [docs/9](docs/9_デプロイ手順書.md) に書いてあります。PostGIS が使える Postgres をゼロスケールで持てる点です。

---

## 実データへの接続

- [ ] `DataSource` の実装を埋める（現在は全メソッドが `NOT_IMPLEMENTED`）
- [ ] `db/init/01_extensions.sql` 相当をクラウド側で実行する
- [ ] `pipeline.py` の出力をファイルではなくDBへ書き込む
- [ ] **店舗マスターの二重管理を解消する** — 現在 `web/src/lib/data/mock.ts` と `scripts/data/stores.json` に同じリストがある（CONTRIBUTING 不変条件①b）

---

## デプロイ

手順は [docs/9](docs/9_デプロイ手順書.md) に揃っています。**未作成のファイルが2つあります。**

- [ ] `web/Dockerfile` と `web/.dockerignore` を作る（内容は手順書に記載済み）
- [ ] `web/next.config.ts` に `output: "standalone"` を追加する
- [ ] GCPプロジェクト作成〜Cloud Run デプロイ
- [ ] Cloud Run Jobs + Cloud Scheduler でバッチを定期実行
- [ ] **予算アラートを設定する**（従量課金のため、バグがそのまま請求になる）
- [ ] 独自ドメインのマッピング
- [ ] GitHub Actions（Workload Identity 連携。**JSONキーは発行しない**）

---

## 公開前に必須

**どれも欠けると公開できません。**

- [ ] **MapTiler のキーを取得する** — OSM公式タイルは規約上、本番利用不可
- [ ] `AUTH_SECRET` を設定する — 未設定だとデプロイのたびに全員ログアウト
- [ ] `ADMIN_SIGNUP_CODE` を設定する — **未設定だと誰でも管理者になれる**
- [ ] 本番URLで**「モックデータ」バッジが出ていないこと**を確認する
- [ ] 非公式である旨・商標の注記が本番画面に出ていることを確認する

---

## クラウドファンディング

- [ ] [docs/7](docs/7_クラウドファンディング本文ドラフト.md) の `【　】` を埋める（実名・経歴・金額・スケジュール）
- [ ] 「なぜ私がこれをつくるのか」を自分の言葉で書く
- [ ] 画面イメージを実画面に差し替える
- [ ] プラットフォームを決める（CAMPFIRE / Makuake）
- [ ] 実測値が出てから公開する — **数字の裏付けがないまま出さない**

---

## 収益化（ユーザーが付いてから）

- [ ] プレミアムの決済を実装する（Stripe想定）。**現在は登録するだけで使える状態**
- [ ] 広告・アフィリエイトの提携（手順は [docs/8](docs/8_広告・アフィリエイト実装ガイド.md)）
- [ ] `ads.txt` の設置とステマ規制対応のPR表記

---

## 積み残し（急がない）

- [ ] パスワード再設定の導線がない
- [ ] `mockup/` は初期ワイヤーフレーム。実装と乖離してきたら削除する
- [ ] 予備調査で見つけた「検索結果に広告投稿が混ざる」問題の除外処理

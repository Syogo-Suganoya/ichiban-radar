# コントリビューションガイド

このプロジェクトに変更を加える前に読んでください。特に「**壊してはいけない不変条件**」は、破ると気づきにくい形で事業側の前提が崩れます。

サービスの概要と事業側のドキュメントは [README.md](README.md) にあります。

---

## リポジトリ構成

```
.
├── docs/      企画書・技術仕様書・事業計画・調査資料
├── web/       Next.js アプリ（フロントエンド + API Routes）
├── scripts/   実測・解析スクリプト（Python）※Docker
├── db/        PostGIS の初期化SQL ※Docker
└── mockup/    初期ワイヤーフレーム（資料用の完成予想図。実装ではない）
```

---

## クイックスタート

### Webアプリ

```bash
cd web && npm install && cp .env.example .env.local && npm run dev
```

| URL | 画面 |
| :--- | :--- |
| http://localhost:3000 | 一般ユーザー向けマップ（閲覧専用） |
| http://localhost:3000/lp | サービス紹介LP |
| http://localhost:3000/admin | 在庫確認コンソール（要ログイン） |

管理画面は初回に `/admin/login` の「新規登録」からアカウントを作ってください。

### 実測スクリプト・データベース（Docker）

Python とデータベースは Docker で立てます。ホストに Python 環境を作る必要はありません。

```bash
cp scripts/.env.example scripts/.env   # APIキーを記入
docker compose build scripts
docker compose run --rm scripts python measure_x.py
docker compose up -d db                # PostGIS（ローカル検証用）
```

APIキーの取得方法は [docs/5_APIキー取得手順と実測手順.md](docs/5_APIキー取得手順と実測手順.md) にあります。

### アーキテクチャ図（Dockerは不要）

図の生成だけはホストで実行します。

```bash
brew install graphviz && pip3 install diagrams && python3 scripts/make_diagram.py
```

---

## 中核ロジック

在庫判定は [`web/src/lib/aggregate.ts`](web/src/lib/aggregate.ts) に集約されています。**単一の投稿を鵜呑みにしないことが、そのままフェイク情報対策になっています。**

- **クロス検証** — 同一店舗の投稿が一致するほど確信度が上がる（1件ごとに +0.04、上限0.97）
- **鮮度減衰** — SNS推測は12時間、電話確認は6時間で失効
- **表示閾値** — 確信度0.4未満は表示しない（空振りによる信頼低下を防ぐ）
- **情報源の重み** — Instagram は店舗特定が弱いため 0.85 倍
- **確定情報の優先** — オペレーターの電話確認はAI推測を上書きする
- **上位賞（A賞）は独立軸** — 在庫数とは別に集計し、「A賞あり」だけで絞り込める

### データの流れ

```
[X API / IG Graph API] → [Gemini解析バッチ] → [Supabase] → DataSource → aggregate() → API Route → UI
```

**差し替え境界は `DataSource`（`web/src/lib/data/source.ts`）の1箇所だけ**です。現在は `MockDataSource`、接続情報を設定すると `SupabaseDataSource` に自動で切り替わります。

---

## 提出前に必ず通すもの

```bash
cd web && npx tsc --noEmit && npx eslint src --max-warnings=0 && npm run build
```

3つすべてが通ることを確認してください。警告も残さない方針です。

---

## 壊してはいけない不変条件

### 1. `Analysis` 型は TypeScript と Python で常に一致させる

AI推測エンジンの出力スキーマは2箇所に存在します。

| 場所 | 用途 |
| :--- | :--- |
| [`web/src/lib/types.ts`](web/src/lib/types.ts) の `Analysis` | アプリ側の型（camelCase） |
| [`scripts/schema.py`](scripts/schema.py) の `Analysis`（Pydantic） | Gemini の Structured Output スキーマ（snake_case） |

**片方だけ変更すると、実データ投入時に静かに壊れます。** 必ず両方を同時に更新してください。
命名規則の変換は `scripts/schema.py` の `to_camel()` が担っているので、フィールドを増やしたらそこにも追加します。

> ⚠️ **`schema.py` の Pydantic モデルだけは `X | None` ではなく `Optional[X]` を使うこと。**
> Pydantic はフィールドの型注釈を実行時に評価するため、Python 3.9 では `str | None` が `TypeError` になります。
> `from __future__ import annotations` があっても回避できません（他のファイルは遅延評価なので新しい記法で問題ありません）。

### 1b. 店舗マスターが2箇所にある（Supabase実装までの暫定）

[`web/src/lib/data/mock.ts`](web/src/lib/data/mock.ts) と [`scripts/data/stores.json`](scripts/data/stores.json) に同じ店舗リストが存在します。
名寄せの検証にはPython側が、画面表示にはTS側が使います。**片方だけ増やすと解析結果が画面に出ない**ので、両方を更新してください。Supabase実装時に解消します。

### 2. オペレーターに「ステータス」を入力させない

管理画面の入力項目は**残り本数と上位賞（A賞）の有無だけ**です。ステータス（完売/品薄/十分）は [`web/src/lib/stock.ts`](web/src/lib/stock.ts) の `deriveStatus()` が導出します。

理由は3つあり、いずれも事業上の判断です（[事業計画書 3-5](docs/3_一番くじ在庫マップ_事業・資金計画書.md)）。

- 入力の手数が単価に直結する（1項目減らすとコストが下がる）
- 「残り8本は品薄か十分か」の解釈を人に委ねるとデータの一貫性が崩れる
- 閾値を後から変えたとき、過去の入力を含めて一括で反映される

**「ステータスも選べた方が便利では」という改善提案は、この3点を覆せる場合のみ検討してください。**

### 3. 判定の根拠を必ず開示する

店舗詳細では、判定の元になった投稿を**そのまま表示**します。「AI推測」「電話確認済み」のバッジと免責表示も必須です。

推測が外れることは前提であり、**ユーザーが自分で判断できる材料を渡すこと**が信頼性の担保になっています。ここを簡略化しないでください。

### 4. 管理画面と一般画面のデザインを混ぜない

[`/admin`](web/src/components/admin/OperatorConsole.tsx) はダーク・高密度・等幅フォント。一般画面はライト・余白多め。**意図的にまったく別物**にしています。

- オペレーターが画面を取り違えて誤操作するのを防ぐ
- 業務画面は1画面で50店舗を捌く情報密度が要る

共通コンポーネント化の誘惑がありますが、**見た目の共通化はしないでください**。

### 5. データソースの差し替え境界を越えない

UIは X API / Instagram / Gemini を**直接呼びません**。必ず [`DataSource`](web/src/lib/data/source.ts) 経由です。

```
[外部API] → [解析バッチ] → [DB] → DataSource → aggregate() → UI
```

UIコンポーネントから外部APIを呼ぶコードを足すと、この分離が壊れて実データ移行が困難になります。

### 6. 一般画面の無料機能を人質に取らない

一般画面（`/`）の**地図・店舗詳細・A賞フィルタは、ログインなしで使えます**。
ここにログインの壁を作ると、母数（＝広告収益とサービスの存在意義）が消えます。

ログインで増えるのは**お気に入りと通知の2つだけ**で、これがプレミアムプランの中身です。
未ログイン時は、使えないボタンを出さずに「何ができるようになるか」を示す導線に差し替えます。

なお**人の手による在庫入力は、在庫確認コンソール（`/admin`）からのみ**行います。一般ユーザーは在庫を報告できません。

### 6b. 通知は「行動が変わる変化」だけに絞る

[`web/src/lib/push.ts`](web/src/lib/push.ts) の `notifyIfCritical()` が送るのは3種類だけです（在庫あり→品薄／→完売／上位賞あり→なし）。

**通知を増やすと即座に解約され、プレミアムの主価値が失われます。** 通知を足したくなったら、それがユーザーの行動を変えるかを先に問うてください。

### 6c. 一般ユーザーと管理画面のセッションを混ぜない

[`web/src/lib/auth.ts`](web/src/lib/auth.ts) は Cookie も署名の名前空間も分けています。
1つのセッションに role を持たせる方式に変えると、権限の取り違えが起きたときの被害が大きくなります。

### 7. オペレーターIDはセッションから取る

[`web/src/app/api/admin/reports/route.ts`](web/src/app/api/admin/reports/route.ts) は、
リクエストボディの `operatorId` を**受け取りません**。セッションから取ります。

他人のIDで入力できてしまうと、入力ログの追跡可能性（事業計画書 3-4）が根本から崩れるためです。

### 8. 認証情報をコミットしない

`.env` / `scripts/.env` / `web/.env.local` は `.gitignore` 済みです。**キーを含むファイルは絶対にコミットしないでください。**

新しい環境変数を足したら、対応する `.env.example` にも**空の値で**追記してください。

---

## コストに直結する変更（要注意）

X APIは**投稿1件の読み取りごとに約0.75円**の従量課金です。取得件数がそのまま原価になります。

以下を変更するときは、[docs/4_データ収集実現可能性調査.md](docs/4_一番くじ在庫マップ_データ収集実現可能性調査.md) を読んでから着手してください。

| 変更内容 | 影響 |
| :--- | :--- |
| `-is:retweet` を外す | **最も危険。** バズ投稿のRT数千件がそのまま課金される |
| 状態語のANDを緩める | 母数が跳ね上がる |
| ポーリング間隔を短くする | `since_id` の差分取得が効いていれば影響は小さいが、UTC日跨ぎで再課金される |
| 検索クエリを長くする | **512文字を超えるとエラー。** 店舗名の列挙は不可能 |

---

## コーディング規約

- **コメントは日本語**。「何をしているか」ではなく「**なぜそうしたか**」を書く
- 事業上の判断が背景にあるコードには、`docs/` の該当箇所を参照として書き残す
- マジックナンバーは名前付き定数にして、閾値の意味をコメントで説明する（`aggregate.ts` / `stock.ts` を参照）
- 型は `web/src/lib/types.ts` に集約する

---

## コミットとプルリクエスト

- コミットメッセージは日本語で、**変更の理由がわかる粒度**で書く
- 事業判断を伴う変更は、`docs/` の該当ドキュメントも同じPRで更新する
- **ドキュメントとコードの数値が食い違った状態を残さない**（例：`LOW_STOCK_THRESHOLD` を変えたら事業計画書 3-5 の表も直す）

---

## 未実装・募集中の領域

| 領域 | 備考 |
| :--- | :--- |
| Supabase（PostGIS）実装 | `web/src/lib/data/supabase.ts` の空実装を埋める。これが済むと店舗マスターの二重管理も解消できる |
| 解析バッチの定期実行 | `scripts/pipeline.py` をCronで回し、出力をDBへ書き込む。本番は Cloud Run Jobs + Cloud Scheduler を想定（[デプロイ手順書](docs/9_デプロイ手順書.md)） |
| 本番デプロイ | Cloud Run + Neon。手順・必要なコード変更・チェックリストは[デプロイ手順書](docs/9_デプロイ手順書.md) |
| パスワード再設定 | 現在は再設定手段がない |
| **プレミアムの決済** | 現在は登録すれば無料でプレミアム機能が使える（画面上で明示済み）。Stripe想定 |
| 地図タイルの本番対応 | 現在はOSM公式タイルにフォールバック。**規約上、本番利用は不可**。MapTilerキーの設定が必要 |
